name = "soulmirror-ai"
main = "ai-proxy.js"
compatibility_date = "2024-01-01"

# 账户 ID 和区域 ID 会在部署时自动检测
# 如果需要手动指定，取消下面的注释并填入你的 ID
# account_id = "your-account-id"

# 环境变量（敏感信息）通过 wrangler secret 命令设置
# 不要在这里明文写入 API Key！
# 使用命令：wrangler secret put DEEPSEEK_API_KEY

[vars]
# 非敏感的环境变量可以在这里设置
# ENVIRONMENT = "production"
